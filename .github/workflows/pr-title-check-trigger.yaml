name:  Ω PR - Title check

on:
  pull_request_target:
    types:
      - opened
      - edited
      - reopened

permissions:
  pull-requests: read

jobs:
  check:
    uses: genesislcap/appdev-workflows/.github/workflows/pr-title-check.yaml@develop
