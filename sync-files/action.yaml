name: Send SSM comamnd to instances
description: Sends an SSM comamnd to the AWS instances based on the Environmnet tag

inputs:
  local-path:
    required: true
  remote-path:
    required: true
  environment:
    required: true
  aws-access-key-id:
    required: true
  aws-secret-access-key:
    required: true
  aws-region:
    required: true

runs:
  using: "composite"
  steps:
    - name: Configure AWS credentials
      uses: aws-actions/configure-aws-credentials@v4
      with:
        aws-access-key-id: ${{ inputs.aws-access-key-id }}
        aws-secret-access-key: ${{ inputs.aws-secret-access-key }}
        aws-region: ${{ inputs.aws-region }}

    - name: Find S3 bucket by tags
      id: find-bucket
      shell: bash
      run: |
        TAG_KEY="Environment"
        TAG_VALUE="${{ inputs.environment }}"

        # Fetch resources with the specified tag key and value
        BUCKET_ARN=$(aws resourcegroupstaggingapi get-resources \
          --tag-filters Key=$TAG_KEY,Values=$TAG_VALUE Key=Type,Values=configs\
          --resource-type-filters s3 \
          --query 'ResourceTagMappingList[0].ResourceARN' \
          --output text)

        if [ "$BUCKET_ARN" == "None" ]; then
          echo "Error: No bucket found with the specified tags."
          exit 1
        fi

        # Extract bucket name from ARN (arn:aws:s3:::bucket-name)
        BUCKET_NAME=$(echo $BUCKET_ARN | cut -d':' -f6)
        echo "Bucket found: $BUCKET_NAME"

        # Set the bucket name as an output
        echo "bucket-name=$BUCKET_NAME" >> $GITHUB_OUTPUT

    - name: Upload the files
      shell: bash
      run: |
        aws s3 sync ./${{ inputs.local-path }} s3://${{ steps.find-bucket.outputs.bucket-name }} --delete --exact-timestamps

    - uses: genesislcap/appdev-workflows/ssm-command@develop
      with:
        command: "aws s3 sync s3://${{ steps.find-bucket.outputs.bucket-name }} ${{ inputs.remote-path }} --delete --exact-timestamps"
        environment: ${{ inputs.environment }}
        aws-access-key-id: ${{ inputs.aws-access-key-id }}
        aws-secret-access-key: ${{ inputs.aws-secret-access-key }}
        aws-region: ${{ inputs.aws-region }}
